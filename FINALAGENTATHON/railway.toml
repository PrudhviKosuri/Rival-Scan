# Railway deployment configuration
[build]
  builder = "NIXPACKS"

[deploy]
  startCommand = "python -m uvicorn core.orchestrator:app --host 0.0.0.0 --port $PORT"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

[variables]
  ENVIRONMENT = "production"
  PYTHONPATH = "/app"
